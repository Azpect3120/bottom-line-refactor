<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/styles/main.css">
    <title>BLBS: 2FA</title>
</head>
<body 
    style="background-image: url('/images/mountains.jpg'); background-position: center; background-repeat: no-repeat; background-size: cover;"
    class="flex items-center justify-center h-screen w-full"  
>

    <!-- Auth Menu -->
    <div class="h-2/5 w-1/4 bg-x-theme-popup-background rounded-2xl flex flex-col items-center">
        <h1 class="text-3xl text-gray-200 mt-4">
            2FA
        </h1>
        <h1 class="text-3xl text-gray-200 font-bold my-4">
            Authentication Code
        </h1>
        <p class="text-lg text-gray-400 text-center">
            Enter the 6-digit authentication code from your mobile/desktop authentication app.
        </p>
        <form action="/login/verify" method="POST" class="h-full w-full flex flex-col items-center justify-evenly">
            <input
                type="text"
                length="6"
                id="token"
                name="token"
                class="bg-transparent outline-none border-b-2 p-2 text-2xl w-2/3 text-blue-300 text-center"
            />
            <button type="submit" class="text-2xl bg-blue-400 text-gray-200 px-8 py-1.5 rounded-2xl"> Verify </button>
        </form>
    </div>

    <!-- Handle error messages -->
    <% if (typeof message !== "undefined") { %>
        <%- include("../pop-ups/errorMessage", { message }) %>
    <% } %>

    <!-- Icon imports -->
    <script type="module" src="https://unpkg.com/ionicons@7.1.0/dist/ionicons/ionicons.esm.js"></script>
    <script nomodule src="https://unpkg.com/ionicons@7.1.0/dist/ionicons/ionicons.js"></script>
</body>
</html>